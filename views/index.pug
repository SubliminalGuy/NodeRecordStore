extends layout

block content
  h1= title

  h2 In Stock

  if error
    p Error getting dynamic content.

  else
    p We have the following Vinyl in Stock:

    ul
      li #[strong Records:] !{data.record_count}
      li #[strong Artists:] !{data.artist_count}
      li #[strong Genres:] !{data.genre_count}
